"use client";

/**
 * Legacy import path — kept so existing `@/components/ui/AppButton` imports
 * keep working. The single source of truth is `@/components/ui/button`.
 */
export { Button, type ButtonProps } from "./button";
